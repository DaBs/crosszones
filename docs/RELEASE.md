# Releasing a new version

To release a new version, bump to the desired version and push from `main` (or whichever branch is intended for release) to `release`.

This will trigger CI automatically releasing based on that push.